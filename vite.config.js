import { defineConfig } from 'vite'

export default defineConfig({
  // Set base to '/' for a custom domain or root deployment.
  // Change to '/repo-name/' if deploying to a GitHub Pages subpath.
  base: '/',
})
